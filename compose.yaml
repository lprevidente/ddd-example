name: orgcraft
services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=myddd'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'